# Question & Answer API

REST API сервис для управления вопросами и ответами, построенный на Go с использованием PostgreSQL.

## 📋 Содержание

- [Технологии](#технологии)
- [Архитектура](#архитектура)
- [Быстрый старт](#быстрый-старт)
- [Конфигурация](#конфигурация)
- [API Endpoints](#api-endpoints)
- [Разработка](#разработка)
- [Тестирование](#тестирование)
- [Структура проекта](#структура-проекта)

## 🛠 Технологии

- **Go 1.25+** - основной язык программирования
- **PostgreSQL 16** - база данных
- **GORM** - ORM для работы с БД
- **net/http** - HTTP сервер
- **Viper** - управление конфигурацией
- **Docker & Docker Compose** - контейнеризация
- **Goose** - миграции БД
- **Testify** - тестирование

## Архитектура

Проект следует чистой архитектуре с разделением на слои:

**Слои:**
- **Handlers** - обработка HTTP запросов, валидация входных данных
- **Services** - бизнес-логика, координация работы репозиториев
- **Repository** - работа с базой данных через GORM
- **Models** - модели данных и DTO

## Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Make
- Go 1.25+ (для локальной разработки)
- Goose (для миграций)

### Установка Goose

```bash
go install github.com/pressly/goose/v3/cmd/goose@latest
```

### Запуск проекта

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd hitalent-test-case
```

2. **Запустите все сервисы:**
```bash
make start
```

Эта команда:
- Соберет Docker образы
- Запустит PostgreSQL и API сервис
- Применит миграции БД

3. **API будет доступен по адресу:**
```
http://localhost:8080
```

### Остановка проекта

```bash
make stop
```

## ⚙️ Конфигурация

Конфигурация находится в файле `config.yaml`:

```yaml
server:
  host: "0.0.0.0"
  port: "8080"
  idle_timeout: 120s
  read_timeout: 10s
  write_timeout: 10s

database:
  host: "db"
  port: "5432"
  user: "dev"
  password: "dev"
  db_name: "dev"
  ssl_mode: "disable"
```

### Переопределение через переменные окружения

Можно переопределить любые настройки через ENV переменные:

```bash
export SERVER_PORT=3000
export DATABASE_HOST=prod-db.com
export DATABASE_PASSWORD=secret123
```

Формат: `SECTION_PARAMETER` (например, `DATABASE_HOST` для `database.host`)

## 📡 API Endpoints

### Questions

| Метод  | Endpoint                | Описание                         |
|--------|-------------------------|----------------------------------|
| GET    | `/api/v1/questions`     | Получить все вопросы             |
| GET    | `/api/v1/questions/:id` | Получить вопрос по ID с ответами |
| POST   | `/api/v1/questions`     | Создать новый вопрос             |
| DELETE | `/api/v1/questions/:id` | Удалить вопрос                   |

### Answers

| Метод  | Endpoint              | Описание            |
|--------|-----------------------|---------------------|
| POST   | `/api/v1/answers`     | Создать новый ответ |
| DELETE | `/api/v1/answers/:id` | Удалить ответ       |

### Примеры запросов

**Создать вопрос:**
```bash
curl -X POST http://localhost:8080/api/v1/questions \
  -H "Content-Type: application/json" \
  -d '{"text": "What is Go?"}'
```

**Получить вопрос с ответами:**
```bash
curl http://localhost:8080/api/v1/questions/1
```

**Создать ответ:**
```bash
curl -X POST http://localhost:8080/api/v1/answers \
  -H "Content-Type: application/json" \
  -d '{
    "question_id": 1,
    "text": "Go is a programming language",
  }'
```

Подробная документация API находится в `specs/openapi.yaml`.

## 🔧 Разработка

### Доступные команды Make

```bash
# Запуск и остановка
make start                   # Полный запуск (сборка + запуск + миграции)
make stop                    # Остановка всех сервисов
make restart                 # Перезапуск без пересборки

# Docker
make docker-compose-build    # Пересобрать образы
make docker-compose-up       # Запустить контейнеры
make docker-compose-down     # Остановить и удалить контейнеры

# Миграции
make create-migration        # Создать новую миграцию
make migrate-up              # Применить миграции
make migrate-down            # Откатить последнюю миграцию

# Тестирование
make test                    # Запустить тесты
make test-cover              # Запустить тесты с покрытием

# Форматирование
make fmt                     # Форматировать Go-файлы
```

### Создание новой миграции

```bash
make create-migration
# Введите имя миграции: add_user_table
```

Будет создан файл в `db/migrations/` с timestamp и именем.

### Применение миграций

```bash
make migrate-up
```

### Откат миграции

```bash
make migrate-down
```

## 🧪 Тестирование

Проект включает unit-тесты для слоя сервисов с использованием mock-объектов.

### Запуск тестов

```bash
# Запустить все тесты
make test

# С информацией о покрытии
make test-cover

# Запустить конкретный тест
go test ./internal/service -run TestCreateQuestion_Success -v

### Примеры тестов

Тесты находятся в `internal/service/service_test.go` и покрывают:
- Успешные сценарии (happy path)
- Валидация входных данных
- Граничные случаи (не найдено, пустые данные)

## 📁 Структура проекта

```
.
├── cmd/
│   └── main.go                    # Точка входа приложения
├── config.yaml                    # Конфигурация приложения
├── db/
│   └── migrations/                # SQL миграции базы данных
├── internal/
│   ├── config/                    # Загрузка конфигурации (Viper)
│   ├── database/                  # Подключение к БД (GORM)
│   ├── handlers/                  # HTTP обработчики (Gin)
│   ├── models/                    # Модели данных и DTO
│   ├── repository/                # Слой работы с БД
│   ├── server/                    # Настройка HTTP сервера
│   └── service/                   # Бизнес-логика
│       └── service_test.go        # Unit-тесты
├── specs/
│   └── openapi.yaml               # OpenAPI спецификация
├── docker-compose.yml             # Композиция Docker сервисов
├── Dockerfile                     # Образ для API сервиса
├── Makefile                       # Автоматизация команд
└── README.md                      # Документация
```

### Описание ключевых директорий

- **`cmd/`** - точка входа приложения, инициализация зависимостей
- **`internal/config/`** - чтение конфигурации из YAML и ENV
- **`internal/database/`** - подключение к PostgreSQL через GORM
- **`internal/handlers/`** - REST API endpoints, обработка HTTP запросов
- **`internal/models/`** - структуры данных, модели БД
- **`internal/repository/`** - CRUD операции с базой данных
- **`internal/service/`** - бизнес-логика
- **`internal/server/`** - настройка net/http сервера
- **`db/migrations/`** - SQL миграции для управления схемой БД
- **`specs/`** - OpenAPI документация (справочная)

## 🐳 Docker

### Образы

- **API Service**: `golang:1.25-alpine3.21` для сборки, `alpine:latest` для runtime
- **PostgreSQL**: `postgres:16-alpine`

### Сети

Сервисы работают в изолированной сети `api-network` (bridge driver).

### Volumes

- `postgres_data` - персистентное хранилище данных PostgreSQL

## 📝 Примечания

### Healthcheck для PostgreSQL

Docker Compose использует healthcheck для проверки готовности БД:

```yaml
healthcheck:
  test: ["CMD-SHELL", "pg_isready -U dev -d dev"]
  interval: 5s
  timeout: 5s
  retries: 5
```

API сервис ждет готовности БД перед запуском (`depends_on: service_healthy`).

### DSN для локальной разработки

Если вы хотите запустить API локально (не в Docker), измените в `config.yaml`:

```yaml
database:
  host: "localhost"  # вместо "db"
```

## 👤 Автор

YotoHana

---

**Полезные команды для быстрого старта:**

```bash
# Первый запуск
make start

# Проверка статуса
docker-compose ps

# Логи API сервиса
docker-compose logs -f api

# Логи БД
docker-compose logs -f db

# Остановка
make stop
```
